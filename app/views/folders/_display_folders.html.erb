<% if @folders.any? %>
  <ul>
    <% @folders.each do |folder| %>
      <li>
      	<i class="fa fa-folder"></i>
      	<%= link_to folder.name, folder %>
      </li>
    <% end %>	
  </ul>
<% else %>
No Files/Folders
<% end %>

