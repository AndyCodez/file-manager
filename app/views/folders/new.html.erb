<%= form_for @folder do |f| %>
  <%= f.label 'Enter folder name:' %>
  <%= f.text_field :name, placeholder: 'Folder Name' %>
  <%= f.collection_select :parent_id, Folder.order(:name),:id, :name, include_blank: true %>
  <%= f.submit 'Save' %>
<% end %>